# Git Remote Detection

This project uses **GitHub** exclusively. Use the `gh` CLI for all remote operations.

## Tool

- Assumes `gh` CLI is installed and authenticated.
- e.g. `gh pr create`, `gh pr list`, `gh issue create`
